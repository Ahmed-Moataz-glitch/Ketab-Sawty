import 'package:flutter/material.dart';

class NotCompletedTabViewWidget extends StatelessWidget {
  const NotCompletedTabViewWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Not Completed Tab View'),
      ),
    );
  }
}