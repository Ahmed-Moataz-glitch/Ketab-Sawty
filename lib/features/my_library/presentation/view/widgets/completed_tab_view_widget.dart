import 'package:flutter/material.dart';

class CompletedTabViewWidget extends StatelessWidget {
  const CompletedTabViewWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Completed Tab View'),
      ),
    );
  }
}